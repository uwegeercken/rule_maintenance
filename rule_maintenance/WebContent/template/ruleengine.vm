<xml> <group id="${rulegroup.nameXMLEscaped}" description="${rulegroup.descriptionXMLEscaped}" outputafteractions="false" dependentgroupid="$!{rulegroup.dependentRuleGroup.name}" dependentgroupexecuteif="$!{rulegroup.dependentRuleGroupExecuteIf}" validfrom="${rulegroup.validFrom}" validuntil="${rulegroup.validUntil}">#foreach($subgroup in ${rulegroup.ruleSubgroups})#if(${rulegroup.ruleSubgroups.size()}>0)	<subgroup id="${subgroup.nameXMLEscaped}" description="${subgroup.descriptionXMLEscaped}" ruleoperator="${subgroup.ruleOperator}"  intergroupoperator="${subgroup.intergroupOperator}">#foreach($rule in ${subgroup.rules})		<rule id="${rule.nameXMLEscaped}" description="${rule.descriptionXMLEscaped}">		<object classname="${project.objectClassname}" method="${project.objectMethodGetter}" parametertype="${rule.object1Parametertype.name}" parameter="${rule.object1Parameter}" type="${rule.object1Type.name}"/>#if(${rule.object2Parameter}&&${rule.object2Parameter}!="")		<object classname="${project.objectClassname}" method="${project.objectMethodGetter}" parametertype="${rule.object2Parametertype.name}" parameter="${rule.object2Parameter}" type="${rule.object2Type.name}"/>#end#if(${rule.expectedValue}&&${rule.expectedValue}!="")		<expected value="${rule.expectedValueXMLEscaped}" type="${rule.expectedValueType.name}"/>#end		<execute value="#if(${rule.check.packageName})${rule.check.packageName}.#end${rule.check.className}">		#if(${rule.additionalParameter}&&${rule.additionalParameter}!="")		<parameter type="${rule.additionalParameterType.name}" value="${rule.additionalParameterXMLEscaped}"/>		#end</execute>		<message type="failed" text="${rule.messageFailedXMLEscaped}" />		<message type="passed" text="${rule.messagePassedXMLEscaped}" />	</rule>#end	</subgroup>#end#end#foreach($action in ${rulegroup.actions})	<action id="${action.name}" description="${action.description}" classname="${action.action.classname}" method="${action.action.methodname}" executeif="${action.executeIf}">#if(${action.parameter1}&&${action.parameter1}!="")					<parameter type="${action.parameter1Type.name}" value="${action.parameter1XMLEscaped}"/>		#end#if(${action.parameter2}&&${action.parameter2}!="")					<parameter type="${action.parameter2Type.name}" value="${action.parameter2XMLEscaped}"/>		#end#if(${action.parameter3}&&${action.parameter3}!="")					<parameter type="${action.parameter3Type.name}" value="${action.parameter3XMLEscaped}"/>		#end		<object classname="${project.objectClassname}" type="setter" method="${project.objectMethodSetter}">			<parameter type="${action.object2Parametertype.name}" value="${action.object2ParameterXMLEscaped}"/>			<parameter type="${action.object2Type.name}" settervalue="true"/>		</object>#if(${action.object1Parameter}&&${action.object1Parameter}!="")		<object classname="${project.objectClassname}" type="getter" returntype="${action.object1Type.name}" method="${project.objectMethodGetter}">			<parameter type="${action.object1Parametertype.name}" value="${action.object1ParameterXMLEscaped}"/>		</object>#end#if(${action.object3Parameter}&&${action.object3Parameter}!="")		<object classname="${project.objectClassname}" type="getter" returntype="${action.object3Type.name}" method="${project.objectMethodGetter}">			<parameter type="${action.object3Parametertype.name}" value="${action.object3ParameterXMLEscaped}"/>		</object>#end	</action>#end</group></xml>