import com.datamelt.db.User;

code=request.getParameter("code");

User dbuser = new User();
dbuser.connection = connection;
dbuser.loadByGeneratedCode(code);

if(dbuser.id==0 || !dbuser.hash.equals(code) || code.trim().equals(""))
{
	infomessage.type="error";
	infomessage.text="Registration error: The provided registration code is invalid";
}
else
{
	context.put("ldap", ldap);
	context.put("dbuser",dbuser);
}
templatename="confirmlogin.vm";

